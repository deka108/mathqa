<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                xmlns:auto="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="@dimen/material_content_edge_margin_horizontal"
                >

    <io.github.kexanie.library.MathView
        android:id="@+id/latex_item_latex"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.AppCompat.Medium"
        android:textColor="@color/material_color_black"
        android:scrollbars="none"
        android:clickable="false"
        android:focusable="false"
        android:focusableInTouchMode="false"
        tools:text="Content"
        auto:engine="KaTeX"/>

    <TextView
        android:id="@+id/latex_item_latex_alt"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:visibility="gone"
        android:text="@string/question_content"
        />

    <TextView
        android:layout_marginTop="@dimen/appbar_padding_top"
        android:id="@+id/sd_solution_latex_alt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        />

</RelativeLayout>